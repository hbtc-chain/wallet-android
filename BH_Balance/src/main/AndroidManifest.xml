<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bhex.wallet.balance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.DelegateActivity"></activity>
        <activity android:name=".ui.activity.TranscationDetailActivity" />
        <activity android:name=".ui.activity.GenerateAddressActivity" />
        <activity android:name=".ui.activity.TransferOutActivity" />
        <activity android:name=".ui.activity.TransferInActivity" />
        <activity android:name=".ui.activity.GatheringActivity" />
        <activity android:name=".ui.activity.AssetDetailActivity" />
        <activity android:name=".ui.activity.CoinSearchActivity" />
        <activity android:name=".BalanceIndexActivity" />
    </application>

</manifest>